/**
 * Display a message on STDOUT.
 * @param {String} message to display.
 * @author abdennour khatib <https://github.com/abdennuur>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
